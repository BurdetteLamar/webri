#!/usr/bin/env ruby

# A utility application to scrape Ruby information from the web.

require_relative '../lib/scraper'

Dir.chdir(WebRI.get_webri_root_dir) do
  release_name = ARGV[0]
  WebRI.check_release_name(release_name)
  scraper = Scraper.scrapers[release_name].new
  scraper.scrape
end
